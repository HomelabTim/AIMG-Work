------add this in the /etc/docker/daemon.json----------

{"hosts": ["tcp://0.0.0.0:2375", "unix:///var/run/docker.sock"]}

------add this in the /etc/systemd/system/docker.service.d/override.conf----------

[Service]
 ExecStart=
 ExecStart=/usr/bin/dockerd --config-file /etc/docker/daemon.json